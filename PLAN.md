# storybook-annotakit — PLAN (long-horizon tracker)

Last updated: 2026-09-22 (v0.6.6 sync-robustness round: 56-finding four-source review → F1–F16 fix clusters across client engine/manager/server/scripts; docs trued to code)

> Note: issue/PR numbers and commit SHAs referenced below belong to a
> PRIVATE development tracker; they may not resolve in the public repo. Full
> per-round records live in worklog.md (staging-excluded).

## Mission

AnnotaKit reborn inside Storybook: zero-setup pin comments on live stories with React component awareness, dev-server-embedded store, agent digests, GitHub issue mirroring. One `addons:` line = the whole review stack. Self-contained.

## Version history

- v0.6.6 (sync-robustness) — **56-finding review (four sources, deduped into fix clusters F1–F16) → P1/P2 fixes across the client engine, manager, server, and scripts — the round that closed the "existing instance can't sync" incident chain (an invalid token override shadowing a provably-valid baked PAT, live-reproduced).** PAT stickiness: token-override VISIBILITY (GhClientStatus.tokenOverridden; honest placeholder "empty keeps the OVERRIDE" + amber warning + "use baked" button → clearTokenOverride() drops ONLY the token override, repo/labels/poll overrides survive; honest when no baked config exists), whitespace-only token input ignored + legacy `token:""` self-heals (resolveGhConfig treats an empty override token as absent — it used to disarm the baked config), manager save diffs against a FRESH ghClientStatus (no more spurious pollMs/repo override writes), follower-tab state surfaced in the settings status line. Follower honesty: syncNow CLAIMS the lease or sets an honest lastError naming it (was a silent no-op fabricating green success with zero network); manual sync clears op backoff + pull backoff + the stale engine error on success (pull warnings — pagination truncation, malformed skips — survive success clears, and buildStatus puts the live engine error ahead of parked/heal errors). Re-bake self-heal: a 401 INVALIDATES the baked-config cache and re-probes (a live tab picks up a re-baked token WITHOUT reload; resolvedBaked kept mid-recovery so status never flickers); a null probe re-arms after a 60s cooldown (SR-W2 killed the forever-latch after a mid-deploy 404). Lease/windows: per-document NONCE in the lease + pagehide conditional release (duplicated-tab W3 guard — two live documents can never both treat the lease as theirs; a reload reclaims instantly; v0.6.5-format nonce-less leases count as expired → rolling upgrade ≤1 TTL, no duel), lease re-verified per flush-loop iteration AND before every remote write (create, each comment — a long drain renews the lease, lifecycle, close; loss → transient re-queue for the new leader; µs both-claim TOCTOU residual accepted — one op of overlap, self-heals), WAKE-LOSS latch (a wake arriving mid-flush is latched `wakePending` and honored by exactly one finishing pass — the re-enable-after-disable deadlock), pull-401 5min backoff (client + server; a manual Sync or settings save clears it). Clocks: syncedAt stamps are SERVER-CLOCK (REST updated_at on create/comment/state-edit, issue.updated_at on pull — client clock skew can no longer permanently hide third-party replies) + future-stamp repair (a legacy skewed stamp >5min ahead re-lists without a `since` filter, ghId dedupe absorbs the overlap). Server: X-Annotakit-Mirror header finally SHIPS (the em dash made Node setHeader throw ERR_INVALID_CHAR silently since v0.6.1; whole-value printable-ASCII sanitization for the dynamic lastError text too), /health + GET /sync gain gh.tokenState ('missing'|'unexercised'|'ok'|'rejected') + lastAuthError and agentSurfaces.githubAuth (outcome-based, additive; presence-based `github` stays), NEW POST /annotakit/api/gh/reload (re-reads .env, applies token changes WITHOUT restart — boot-applied keys only, shell vars keep precedence, vanished keys removed, requiresRestart discloses boot-latched repo/labels/poll/interval/auto, PAT never echoed; registered in /schema endpoints + KNOWN_API_ROUTES; the SR-W2 fix makes the first-time-token branch register as boot-applied so EVERY later rotation flows), ghsync installs timers even when unconfigured at boot (a token dropped into .env + reload goes live without restart; off-mode stays restart-only + disclosed), enqueueDelete's pull-kick surfaces auth errors instead of swallowing them, token rotation resets rejected→unexercised (both directions off missing, SR-W2) AND clears a 401 backoff, git-push auth failures classified ("rotate ANNOTAKIT_GH_TOKEN in .env, then POST /annotakit/api/gh/reload"). Scripts: heal-mirrors dev-format builders use the BARE origin (the forced trailing slash made the dev-format heal structurally dead since v0.6.5 — history's storybook: line carries no slash; the static format keeps the slashed origin); gh.ts token messages name the reload endpoint instead of RESTART-only. Tests: suites extended with regressions (lease nonce incl. pagehide reclaim, per-op lease re-verification, wake latch, 401 re-probe + null re-arm, pull-401 backoff, server-clock stamps, reload mechanics, tokenState transitions; counts self-report — ship-time numbers live in the README status line + HANDOFF). Known non-goals: remote comment DELETIONS are not detected by either engine (pre-existing); F15 static-panel delete affordance DEFERRED (25–40 lines + destructive confirm dialog — not this round).
- v0.6.5 (hardening) — **three-wave review (8 audit tracks, triage clusters C01–C40) → ~40 P1/P2 fixes; docs trued to code.** Mirror self-heal contract CHANGED to EXACT-MATCH: a mirror body is rewritten only when it byte-equals the frozen v0.5.0–v0.6.2 machine render (LEGACY-A v0.5.0–v0.6.0 / LEGACY-B v0.6.1–v0.6.2, both engines, one shared `src/shared/legacyMirror.ts`; title only on exact equality with the legacy builder; the server's render-time date is normalized) — the v0.6.4 heuristics (strict title-prefix, never-shorten) could destroy human edits (H-B-01: a human edit under the rebuild length was silently overwritten; H-B-08: human-truncated titles passed the prefix test) and are gone; a human-edited mirror never matches (safe miss — stays old-format, replies still flow); idempotent by construction (healed body carries the verbatim marker). Engine recoveries: create-crash ORPHAN ADOPTION (a stamped unmapped issue is adopted, not duplicated — push + pull paths), requeue-during-flight, 422-parked client ops UNPARK on new thread content (config saves never unpark), push-path 404 unstick (issue deleted remotely → mapping resets → fresh issue; no more threadPending deadlock), cross-tab leader lease (sessionStorage tab id + localStorage lease — two manager tabs never double-flush), client 30s stalled sweep (unmapped/un-mirrored threads re-enqueue), phantom-reopen guard, staticStore comment-union + gh preservation + explicit unlinkGh engine door, remote-fallback store branch, SIGTERM engine drain before the final git snapshot, PAT via GIT_CONFIG env (never argv — was world-readable via /proc). API: PATCH deep target validation + comment re-hash (ids new to the thread) + client-gh strip, null/array/malformed bodies are 400s (were 500s), loopback-first API gate (loopback ALWAYS passes — ANNOTAKIT_API_KEY gates non-loopback only; 401 vs 403 now consistent), %-malformed id 400, json-store read-refusal (unreadable content refuses mutations loudly — no silent wipe). Budget/hygiene: pagination 10-page cap + LOUD truncation warning (was silent), rate-limit clock-skew clamp (past x-ratelimit-reset no longer yields negative backoff), digest metadata lines single-lined + capped 300/400 (no structure injection into digests/mirrors), release-check dist-FRESHNESS gate (stale dist fails the release; selftest proves it + restores in finally), heal errors persisted client-side + healed count surfaced in the panel. Tests: api 99→107, engine 76→82, ghclient 88→98, static 41→48, store 11 (all self-report; typecheck clean). Docs truth pass: H-G-01..10 fixed (loopback gate wording, .env default 700ms, removed-chips row, pin-with-popup lesson, allowlist note, PLAN date, 48.9k stat alignment, private-repo name scrubbed, autoSync blame corrected, counts principle re-worded). Known residuals were flagged at docs time (scripts/, out of docs scope) and BOTH were fixed in the SAME release (f6f0419 wave 5, verified): scripts/heal-mirrors.mjs now delegates to the shared exact-match decision (no more v0.6.4 heuristics), and the ENV_EXAMPLE template in scripts/stage-release.mjs documents ANNOTAKIT_GH_INTERVAL as 700 (the actual routes.ts default).
- v0.6.4 (mirror self-heal) — **existing truncated mirrors repair themselves + panel stickers removed.** Issue #16 follow-up ("re-sync existing issues that has truncated"): both engines (ghsync pull + ghClient pullOnce) detect pre-v0.6.3 mirrors using the body the pull already fetched (zero extra requests) and editIssue the verbatim body + 100-char title in place — so every downstream deployment (e.g. the reporter's repo) heals on its first sync after upgrade, no operator action. Safety contract (identical both engines): title heals only when remote is a STRICT PREFIX of the wanted title (60→100 budget makes old titles exact prefixes; human edits aren't prefixes); body heals only when it still carries the `- thread id:` stamp, LACKS MIRROR_VERBATIM_MARKER, and the rebuild is not shorter (never shorten — human appends survive); idempotent by construction (marker present after one heal). *(Superseded in v0.6.5 by the exact-match contract — see the v0.6.5 entry above: the heuristic could destroy legitimate human edits.)* `POST /sync` reports `healed`; scripts/heal-mirrors.mjs (dry-run default, --apply) backfills orphaned repos, matching by thread-id stamp IN THE BODY — never by issue number (numbers collide across repos; the same thread mirrors to different numbers). Panel: persistent `static → github` / `static · local-only` chips deleted (user directive "sticker overlay is still there"); status rides affordances — GitHub-button dots (green live / amber off-with-queue / red error), sync-button `sync · N` while draining, tooltip + settings carry the details. Builders exported (mirrorIssueTitle/mirrorIssueBody, renderDigest re-export from server entry, MIRROR_VERBATIM_MARKER/ISSUE_BODY_LIMIT re-export from ghClient) so the script can't drift. Tests: engine 68→76, ghclient 82→88 (heal + idempotence + human-rewrite negative controls both engines; fake GHs model PATCH title/body). Ops: -dev repo healed 2/2 via the script; public repo healed via script + live browser session.
- v0.6.3 (review-flow) — **`fixed` status + full-text mirrors + overlay-free canvas.** open → fixed (agent addressed) → resolved (reviewer confirms): blue pins/`to review` filter/`recent` sort (slice 1, updatedAt DESC)/badge fixed-aware; monotonic merge open<fixed<resolved; mirror: fixed keeps issues OPEN, close = reviewer-confirmed FROM open-or-fixed, reopen only from resolved (the aborted-session reopen bug pre-empted by design); resolved→fixed 400-guard; counts.fixed additive + ?status=fixed; footers flip Path A/B (agents do NOT close). Issue #16: issue bodies carry VERBATIM bodies (full-text thread blocks, both engines), title headline 60→100, 60k honest clip vs GitHub's 65,536 cap. Canvas: static chip + capture banner removed (user directive), z-stack pins>passive<interactive. Tests +33 checks (api 99, engine 68, client 82, static 41, store 11). Found-by-tests: staticStore.patch demotion resurrected resolvedAt via spread; ghclient create-return predates issue-body stamp (patch fresh docs only).
- v0.6.2 (docs) — **deflate round:** SKILL.md 48.9k→28.3k public form (Read-tool one-pass, empirically verified; ~140 load-bearing facts preserved per 2 audits + cold-start mission test), README 29.7k→25.1k, PLAN compacted. Process: line-by-line inventory → dual-stance sub-agent verdict review (fat-hunter + loss-guard) → drop → condense → info-loss audit → cold-start over/under-teaching test. stage-release: 7 prose-anchor transforms → 1 structural §8 cut. Stale count claims eliminated docs-wide (suites self-report).
- v0.6.1 — **hardening round** after a 4-track audit (robustness, cold-consumer friction, human UX, docs/release) + design-critique round. P0: local-only tab freeze (unconditional `finally` re-arm → infinite microtask chain; gated on did-work + regression tests). Agent contracts: PATCH status enum, replay flags, 64k caps at every door (PATCH exempts stored bodies), real 413 for ≥2MB (was ECONNRESET — respond before destroy), 404 hints, slash tolerance, format 400s, /schema endpoints index. Git durability: machine-readable `/health` git block, durability degrades honestly, logOnce re-logs at 1/5/25/100, A14 no-readme/foreign split, versioned store marker `annotakit-store: v1`, POST /sync forces the git cycle. Client: backoff-clear on Save/Reset, 422 parking, quota surfacing, pin-with-popup-open, two-stage Esc, keyboard pin dots, name suppression, digest clipping + provenance. Release hygiene: npm files whitelist (v0.6.0 tarball would have leaked the staging script + untransformed SKILL), release-check gate (selftested), stage-release sweep hardening. Battery: api 88/88, ghclient 77/77, static 31/31, store 11/11, engine 61/61; 12-a/12-b verification rounds all-pass.
- v0.6.0 — **first public release** (one clean commit + tag + notes + topics; triple secret audit). Final-review found one real bug: client pull label filter as repeated `labels=` params = GitHub last-wins → AND contract silently broken since v0.5.3; fixed to comma form, fake harness models real semantics, regression test. Onboarding-test doc fix: POST example now carries the idempotency key (top-level `id`) — retrying agents were minting duplicates.
- v0.5.3 — **client-side GitHub publishing for static builds** (user-directed: delivery beats PAT secrecy; the browser owns the mirror). ghClient.ts: baked `annotakit-gh.json` + localStorage overrides, outbox-before-network (boot drain/wake/30s sweep), leader-only flusher, full lifecycle parity with the engine, pull import with ghId dedupe + self-heal. E2E through the public preview incl. the dead-server reply proof. Original security posture (sidecar proxy) stands as the DEFAULT-NO reversal path: the sidecar can become the transport behind the SAME ghClient interface (swap ghJson's apiBase + token resolution).
- v0.5.2 — adoption-hardening from the second downstream stream (issue #18/PR #19): 7 live-traffic fixes + shutdown flush JOINS in-flight cycles (root-caused the ~25% divergence flake) + engine-suite sentinel assertions; both #18 suggestions shipped.
- v0.5.1 — subdir-project fix from a live downstream consumer (issue #16/PR #17): cwd-relative ls-tree pathspec read every store branch as foreign; fixed with colon-paths; store-robustness `subdir` case pins it.
- v0.5.x — static build support stages 1+2 (baked seed, localStorage store, per-deployment scoping, auto mode detection).
- v0.5.0 — native SB toolbar + ⌥-hotkeys + DOM-inspector precision + plan-b DOM snapshots; gitdir store + orphan-branch durability + logical union merge (design A1–A15 in design/2026-09-05-store-robustness.md).
- v0.4.1 — dogfood hardening (sourcemap-corrected jsx lines, DELETE path form, story-wrapper naming, bootedAt).
- v0.4.0 — production hardening (serialized engine, backoff, 404-heal, local mode first-class).
- v0.3.0 — 1:1 GitHub lifecycle mirror, idempotent, bidirectional.
- v0.2.0 — all 10 user-reported frictions fixed. v0.1.0 — core addon.

## Shipped rounds — archived scope

- **v0.6.1 fix plan** (4 phases, all shipped, per-phase detail in worklog Task 11): P1 flushOnce ranWork gate + suppressed chip + backoff-clear + 422 parking + quota surfacing; P2 routes (readBody respond-before-destroy, status enum, MAX_BODY_CHARS doors, 404 hints, slash strip, /schema index, replay flags, format 400s); P3 sync.ts (gitHealth, counting logOnce, A14 split, store marker v1, syncNow, durability degrade); P4 client UX (chip truth, Esc two-stage, pin-dot a11y, hint/quota banners, composer caps); P5 release hygiene (files whitelist, release-check, stage-release hardening, docs truth).
- **v0.5.0 scope** (all [x], shipped 024bb11..): A. UX (toolbar TOOL buttons over UI_COMMAND; Alt/⌥ hotkeys via e.code; elementSummary identity line; plan-b DOM snapshot — text beats pixels, own table, 96KB cap); B. gitdir store + orphan branch + logical union + boot restore + shutdown flush (design doc + A1–A15); C. dogfood issues #2/#3/#4/#6/#7/#8/#9/#10/#11 (target validation, loopback-only CORS + API key, partial PATCH, boot hygiene warnings, dist git-tracked, /schema).
- **Upstream feedback backlog (issue #16)** — all six shipped in v0.6.1 (git health machine-readable, counting logOnce, A14 split, POST /sync forced cycle, versioned README marker, release-check chunk gate; store-robustness `githealth` + `marker` cases pin them).

## Deferred / next horizon

- static-panel delete affordance (F15, deferred from v0.6.6: 25–40 lines + a destructive confirm dialog; the close-op enqueue path already exists — wire linked.deleteThread in the manager)
- npm publish (SAFE since v0.6.1: files whitelist + pack guard + release-check all pass; stage from a staged tree or verify pack first)
- test-suite expected-total self-assertion (each script pins its own check count — catches silent check deletion; docs quote ship-time counts in the README status line + HANDOFF, so a silent deletion lowers the self-reported total with nothing failing)
- MCP server surface for agents (REST + digests suffice for now)
- per-pin video/interaction capture (play fn recording)
- GH issue attachments for DOM-snapshot evidence (needs user-assets API workaround)
- react-native / non-DOM story support (out of scope until asked)
- remote collaborator presence (who's reviewing now)

## Principles (sticky)

1. GIT IS THE DISK — push every micro milestone
2. NEVER force push; fix divergence, or stop for explicit permission
3. keep a stable checkout path (container overlays/recycles must not move the repo)
4. design decisions → written down → sub-agent critique round(s) → implement
5. every change lands with tests; every release with docs + version bump
6. docs quote battery counts only as ship-time records (README status line, HANDOFF) — the suites self-report live totals; expected totals still belong in the scripts (deferred self-assertion)
